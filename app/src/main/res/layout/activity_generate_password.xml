<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.views.GeneratePasswordActivity">

        <TextView
            android:id="@+id/textSelectTypePassword"
            android:layout_width="match_parent"
            android:text="@string/txtSelectPassword"
            android:textColor="@color/teal_blue"
            android:gravity="center"
            android:textSize="20sp"
            android:layout_height="0dp"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="50dp"
            android:layout_marginHorizontal="20dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Spinner
            android:id="@+id/planets_spinner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginHorizontal="20dp"
            app:layout_constraintEnd_toEndOf="@+id/textSelectTypePassword"
            app:layout_constraintStart_toStartOf="@+id/textSelectTypePassword"
            app:layout_constraintTop_toBottomOf="@+id/textSelectTypePassword" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
